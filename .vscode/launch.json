{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Python: Standalone",
            "type": "python",
            "request": "launch",
            "program": "${workspaceFolder}/standalone.py",
            "console": "integratedTerminal"
        }
    ]
}
